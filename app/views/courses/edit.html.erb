<% provide(:title, "Edit #{@course.description}") %>

<%= render 'courses/side_menu' %>
<div class="course-content">
  <div class="course-title">
    <h1>Edit <%= @course.description %></h1>
  </div>
  <%= render 'form' %>
</div>
